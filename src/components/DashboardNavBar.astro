---
import PillButtonGroup from "@/components/shared/PillButtonGroup.astro";
import Button from "@/components/shared/Button.astro";

const dashboardNavItems = [
  { label: "Товары", href: "/dashboard", match: "exact" as const },
  { label: "Заказы", href: "/dashboard/orders", match: "prefix" as const },
  // {
  //   label: "Категории",
  //   href: "/dashboard/categories",
  //   match: "prefix" as const,
  // },
];
const isDashboardFeedPage = Astro.url.pathname === "/dashboard";
---

<nav class="fixed bottom-0 left-0 right-0 z-999 flex gap-2">
  <PillButtonGroup items={dashboardNavItems} />
  {
    isDashboardFeedPage && (
      <Button size="sm" href="/dashboard/product/new">
        Создать
      </Button>
    )
  }
</nav>

<style>
  nav {
    padding: 0.375rem;
    /*background: linear-gradient(
      transparent 0%,
      color-mix(in oklch, var(--background) 1%, transparent) 30%,
      color-mix(in oklch, var(--background) 4%, transparent) 45%,
      color-mix(in oklch, var(--background) 10%, transparent) 58%,
      color-mix(in oklch, var(--background) 22%, transparent) 70%,
      color-mix(in oklch, var(--background) 40%, transparent) 82%,
      color-mix(in oklch, var(--background) 62%, transparent) 92%,
      color-mix(in oklch, var(--background) 80%, transparent) 100%
    );*/
  }
</style>
