---
import DashboardLayout from "@/layouts/DashboardLayout.astro";
import { listDashboardProducts } from "@/db/dashboard/products/listProducts";
import DashboardProductCard from "@/components/dashboard/DashboardProductCard.astro";

const products = await listDashboardProducts();
---

<DashboardLayout title="Ансин — Админка">
  <main>
    <div
      class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 gap-4"
    >
      {
        products.map((product) => (
          <DashboardProductCard
            title={product.name}
            slug={product.slug}
            images={[product.first_image, product.second_image].filter(
              (path): path is string =>
                typeof path === "string" && path.length > 0,
            )}
            isActive={product.isActive}
          />
        ))
      }
    </div>
  </main>
</DashboardLayout>
